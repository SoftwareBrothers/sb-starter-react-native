import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  formField: {},
  formFieldWrapper: {
    borderColor: 'black',
    borderWidth: 1,
  },
  formFieldWrapperError: {
    borderColor: 'red',
  },
})
